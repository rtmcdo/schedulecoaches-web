{
  "name": "schedulecoaches-api",
  "version": "1.0.0",
  "description": "Backend API for schedulecoaches.com - handles authentication, Stripe checkout, and subscription management",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@azure/functions": "^4.0.0",
    "stripe": "^14.0.0",
    "mssql": "^10.0.0",
    "jsonwebtoken": "^9.0.2",
    "jwks-rsa": "^3.1.0",
    "google-auth-library": "^9.0.0"
  },
  "devDependencies": {
    "@types/jsonwebtoken": "^9.0.5",
    "@types/mssql": "^9.1.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "azure-functions-core-tools": "^4.x"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "main": "dist/app.js"
}
